from core.utils import BaseAgent

class RiskAssessorAgent(BaseAgent):
    def run(self , input_data):
        # placeholder: Simulate risk analysis

        print(f"{self.name}: Assessing financial risk...")
        return {"risk": "sample risk"}